# MLOps Assignment 4

This project trains a simple MNIST neural network using PyTorch and logs metrics with MLflow.

## Steps

1. Install dependencies: `pip install -r requirements.txt`
2. Run training: `python train_mnist_mlflow.py`
3. MLflow UI: `mlflow ui` to visualize experiments.